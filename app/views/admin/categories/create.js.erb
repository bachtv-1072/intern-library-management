<% if @status %>
  toastr.success('<%= t 'categories.message.sucsses'%>')
  $('.test').append("<%= j render 'category', category: @category,
    category_counter: @index %>");
<% else %>
  toastr.error('<%= t 'categories.message.failer'%>')
<% end %>
